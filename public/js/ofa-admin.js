// Fallback (dev mode) - If you haven't built assets with Vite yet, this will mount the ThemePage
console.log('OFA admin script loaded. For full functionality, run `npm install` and `npm run dev` to build assets (Vite).');
